Name:           ros-hydro-object-recognition-capture
Version:        0.3.0
Release:        0%{?dist}
Summary:        ROS object_recognition_capture package

Group:          Development/Libraries
License:        BSD
URL:            http://wg-perception.github.io/capture
Source0:        %{name}-%{version}.tar.gz

Requires:       boost-devel
Requires:       ros-hydro-ecto
Requires:       ros-hydro-ecto-image-pipeline
Requires:       ros-hydro-ecto-opencv
Requires:       ros-hydro-ecto-openni
Requires:       ros-hydro-ecto-ros
Requires:       ros-hydro-object-recognition-core
BuildRequires:  boost-devel
BuildRequires:  ros-hydro-catkin
BuildRequires:  ros-hydro-ecto
BuildRequires:  ros-hydro-ecto-image-pipeline
BuildRequires:  ros-hydro-ecto-opencv
BuildRequires:  ros-hydro-ecto-openni
BuildRequires:  ros-hydro-object-recognition-core

%description
Capture is a set of tools to capture objects in 3D and perform odometry

%prep
%setup -q

%build
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/hydro/setup.sh" ]; then . "/opt/ros/hydro/setup.sh"; fi
mkdir -p build && cd build
%cmake .. \
        -UINCLUDE_INSTALL_DIR \
        -ULIB_INSTALL_DIR \
        -USYSCONF_INSTALL_DIR \
        -USHARE_INSTALL_PREFIX \
        -ULIB_SUFFIX \
        -DCMAKE_INSTALL_PREFIX="/opt/ros/hydro" \
        -DCMAKE_PREFIX_PATH="/opt/ros/hydro" \
        -DSETUPTOOLS_DEB_LAYOUT=OFF \
        -DCATKIN_BUILD_BINARY_PACKAGE="1" \

make %{?_smp_mflags}

%install
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/hydro/setup.sh" ]; then . "/opt/ros/hydro/setup.sh"; fi
cd build
make %{?_smp_mflags} install DESTDIR=%{buildroot}

%files
/opt/ros/hydro

%changelog
* Sun Jul 27 2014 Vincent Rabaud <vincent.rabaud@gmail.com> - 0.3.0-0
- Autogenerated by Bloom

